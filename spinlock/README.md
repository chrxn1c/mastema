# SpinLock

An implementation of basic synchronization primitive, spin-lock, as a library crate. Based on threads (not async).

> Note: All platforms are supported

## Testing

> Note: Have to be in workspace root

- Run the tests: `$ cargo test --lib spinlock`
